# Modulos C++ proprios

Adicione aqui as bibliotecas C++ do projeto, por exemplo:

```text
modulos/logger/
modulos/string-helper/
```

Cada modulo pode ter seu proprio `CMakeLists.txt`, `include/` e `src/`.

